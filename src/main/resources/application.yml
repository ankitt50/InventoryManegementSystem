spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: Password
        url: jdbc:postgresql://localhost:5432/MobileProgramming
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true